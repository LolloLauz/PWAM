import { OrdinazioneRequest } from './ordinazione-request';

describe('OrdinazioneRequest', () => {
  it('should create an instance', () => {
    expect(new OrdinazioneRequest()).toBeTruthy();
  });
});
